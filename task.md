# 任务背景（参考说明）
说明：如果你有展板、团体照、证书照片或初版网站链接，可在后续提交给审阅者；如果当前没有，请直接以“通用校园技术社团”为参考出方案，并在方案中明确列出需要的素材清单与规格（例如高清证书、团队照、项目截图等）。
这些是已有奖项证书截图[text](云飞扬奖项)

目标概述：基于已有的暗色 Awwwards 风格初版（情绪驱动、高质感），完成以下工作：
1. 保留并优化暗色主题（Midnight Code）；  
2. 新增一套**仿 Claude 风格的亮色主题**（提高可读性、亲和感），并支持主题切换（跟随系统偏好 + 用户切换 + 本地持久化）；  
3. 以“对外展示（面向在校学生）”为主线，兼顾品牌质感、叙事化案例、可读性、性能与可维护性。

---

# 要求（本次仅产出“方案”）
**请注意**：本次输出必须是**详细的实施方案（Plan）**，格式为 Markdown，能直接交付给设计/前端/运营/QA 团队拆解执行并估算工作量。**不要**在本次输出中生成完整前端运行代码（除非在方案末尾给出极短的示意片段）。方案须包含下列全部内容。

---

## 1. 高级摘要（1 段）
- 简述设计目标、用户画像（在校学生）、核心 KPI（例如：首页 CTA 点击率、报名转化率、页面停留时长、移动端首屏加载时间等）。

## 2. 信息架构与页面清单
- 列出需实现页面与模块（例如：Home / About / Tech / Achievements / Project Case / Team / Join / Footer），并为每页给出职责/情绪目标与主要组件清单（Hero、证书墙、项目 Case、团队卡、加入表单等）。

## 3. 视觉与主题规范（必须包含）
- **两套主题的设计 tokens（完整 CSS 变量）**：亮色（仿 Claude）与暗色（Midnight Code）。请给出颜色、阴影、边框、卡片背景、文本/次文案/muted 色、CTA 主色/次色的具体 hex / rgba 值（tokens 可直接作为 CSS 片段复制）。
- **字体与排版规则**（中文/英文字体栈、响应式字号样例、行高、标题层级）。
- **配图/图片规范**（证书裁切比例、团体照裁切、导出格式 WebP/AVIF、LQIP/占位方案、推荐分辨率与最大体积）。

## 4. 交互动效规范（Motion System）
- 全站统一的动效规范（入场/hover/scroll/reveal/hero 解构/降级策略），并给出每类动效的 duration、easing、delay、触发条件（IntersectionObserver / CSS hover / JS scroll），以及 `prefers-reduced-motion` 的降级策略。
- 说明桌面与移动端差异（哪些交互仅在桌面启用，移动端如何降级）。

## 5. 关键组件设计说明（逐项列出并指明交付物）
# 云飞扬社团官网 v3.0 开发任务列表

## Phase 1: Infrastructure
- [ ] 1. Theme 基础设施 (feature/theme) <!-- id: 101 -->
  - [ ] CSS Tokens (Dark/Light)
  - [ ] useTheme Composable
  - [ ] Init Script (No flash)
- [ ] 2. ThemeToggle 组件 (feature/theme-toggle) <!-- id: 102 -->

## Phase 2: Core Components
- [ ] 3. Hero 组件升级 (feature/hero) <!-- id: 103 -->
- [ ] 4. CertificateWall 组件 (feature/certificate-wall) <!-- id: 104 -->
- [ ] 5. JoinForm 组件 (feature/join-form) <!-- id: 105 -->

## Phase 3: Analytics & SEO
- [ ] 6. GA4 基础埋点 (feature/ga4) <!-- id: 106 -->
- [ ] 7. SEO JSON-LD (feature/seo) <!-- id: 107 -->

## 11. 工作分解（可交付任务分解）
- 将方案拆成若干可交付任务（设计、前端、内容、QA、部署、数据），每项给出工作清单与建议优先级（高/中/低）。**（不要给出硬性日程，但可给“工时范围建议”以便估算）**

## 12. 输出格式要求（必须）
- 请以 Markdown 返回，层级清晰；  
- 每个组件或页请包含**可复制的 CSS 变量片段**（至少提供两套主题 tokens），并给出关键动效的时序表（duration/easing/delay/trigger）；  
- 至少给出 2–3 个关键交互的伪代码或小示意片段（JS 或 CSS，**仅作示意**）；  
- 在结尾附上“交付清单（可复制粘贴的文件名 + 格式）”与“所需素材清单与规格”。

---

# 产出示例（参考结构）
1. 概要  
2. 页面与模块清单  
3. 设计 tokens（亮色 + 暗色 CSS） ← 必要  
4. Hero 规范（暗/亮）  
5. 证书墙规范  
6. Case Study 页面模板与内容模版  
7. 动效系统与降级策略  
8. 技术栈与部署方案  
9. 性能/无障碍/SEO 要求  
10. 埋点与 A/B 测试计划  
11. 交付清单与验收标准  
12. 风险与替代方案  
13. 任务拆解（设计/前端/运营/QA/数据）

---

# 语言与风格
- 语言：**简体中文**  
- 风格：面向工程与产品的可执行方案（条理清晰、要点明确、便于交付给团队执行）。  
- 输出尽量结构化（要点列表、表格或代码块），方便直接转交给设计/前端/运营/QA。

---

# 最后说明
请**只输出详细实施方案（Plan）**，并在方案结尾附上“交付清单（文件名 + 格式）”与“所需素材清单（清晰度/尺寸/格式）”。如果需要任何附加约束（例如 **必须使用 Next.js** 或 **禁止使用某些库**），请在调用时一并说明。  
